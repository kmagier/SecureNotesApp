version: '3'
services:
  web:
    build:
      context: .
      dockerfile: ./Docker/app/Dockerfile
    ports:
      - "8080:80"
    volumes:
      - ./application:/application
    env_file: 
        - ./application/.env
  redis:
    image: "redis:alpine"